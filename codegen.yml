overwrite: true
schema: "http://localhost:8000/___graphql"
generates:
  src/types.tsx:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
config:
  namingConvention:
    enumValues: keep