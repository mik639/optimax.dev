overwrite: true
schema: "http://localhost:8000/___graphql"
generates:
  src/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
config:
  namingConvention:
    enumValues: keep